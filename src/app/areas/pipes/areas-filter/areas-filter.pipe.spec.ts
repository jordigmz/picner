import { AreasFilterPipe } from './areas-filter.pipe';

describe('AreasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AreasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
