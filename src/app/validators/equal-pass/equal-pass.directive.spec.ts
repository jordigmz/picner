import { EqualPassDirective } from './equal-pass.directive';

describe('EqualPassDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualPassDirective();
    expect(directive).toBeTruthy();
  });
});
