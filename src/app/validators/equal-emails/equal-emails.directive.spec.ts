import { EqualEmailsDirective } from './equal-emails.directive';

describe('EqualEmailsDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualEmailsDirective();
    expect(directive).toBeTruthy();
  });
});
