import { EqualMailDirective } from './equal-mail.directive';

describe('EqualMailDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualMailDirective();
    expect(directive).toBeTruthy();
  });
});
